# Kernel Events Kernel Module

This simple ioctl module is for sending messages between processes using the kernel.

Contains the kernel module plus a test app to send data

## TODO:
[ ] Poll for data  
[ ] Thread safety
